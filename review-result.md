# Swarm Review ✅ (with comments)

**Verdict:** approved_with_comments | **Risk Tier:** full | **Diff:** 319 lines, 5 files

## Summary

Clean diff with no critical security or production-safety issues. The main concern is a **breaking API change** (home zone immutability) deployed without migration path, and **documentation drift** across README/AGENTS.md. Several low-severity code quality notes. No action is blocking.

## Findings

### 🟡 **WARNING**: /set-home-zone now permanently immutable — no migration path

- **Category:** api / documentation / release
- **File:** `server/src/routes/auth.ts`

The endpoint now returns `403` if `homeZoneId` is already set. This is a breaking behavioral change — any client that allowed users to change their home zone will now silently fail. No deprecation notice, migration guide, or version bump.

**Recommendation:** Add migration notes to README. Bump version to at least 1.1.0.

---

### 🟡 **WARNING**: Economy rebalance — daily bonus 50→500 xu, bot seed balance 999999→0

- **Category:** economy
- **Files:** `server/src/progression.ts`, `server/prisma/seed.ts`

Daily login reward increased 10× without offsetting sinks. Bot seed balances reset to 0 which is an intentional design change (bots don't deduct balance). Worth documenting the new economy parameters.

**Recommendation:** Document new economy numbers in README.

---

### 🔵 **SUGGESTION**: Speed fallback value not updated after rebalance

- **Category:** quality
- **File:** `server/src/bots.ts`

The SPEED table was roughly doubled but the fallback `(SPEED[vehicle] || 0.000270)` still uses the old pre-rebalance value. Any shipper with an unexpected vehicle string would move at ~half speed.

**Recommendation:** Update fallback to `0.000550` to match new Xe Máy default.

---

### 🔵 **SUGGESTION**: README seed counts and account table outdated

- **Category:** documentation
- **File:** `README.md`

README says "8 shops, 7 shippers, 10 buyers" but seed now produces 10/35/15. Seeded accounts table missing `pho_hanoi` and `bbq_garden`.

**Recommendation:** Update README to reflect current seed data.

---

### 🔵 **SUGGESTION**: No root AGENTS.md — AI agents have no project context

- **Category:** agents-md
- **File:** (none exists)

The project has no root `AGENTS.md`. `client/AGENTS.md` is a 4-line Next.js auto-generated stub. AI coding agents have no guidance on stack, run commands, bot architecture, or API behavioral rules.

**Recommendation:** Create a root AGENTS.md covering stack, run commands, seed process, bot architecture, and API constraints.

---

### 🔵 **SUGGESTION**: useEffect missing `homeZoneId` dependency

- **Category:** quality
- **File:** `client/app/buyer/page-content.tsx`

The effect reads `user.homeZoneId` but was deliberately excluded from the dependency array. React exhaustive-deps would flag this. Low practical impact since the page reloads after zone selection.

**Recommendation:** Restore `user?.homeZoneId` to the dependency array.

---

## Reviewer Stats

| Reviewer | Findings |
|----------|---------|
| security | 1 (TOCTOU in set-home-zone, low) |
| performance | 0 |
| code-quality | 5 |
| documentation | 5 |
| codex | 3 |
| agents-md | 5 |
| release | 5 |

---

*Generated by swarm-review*
